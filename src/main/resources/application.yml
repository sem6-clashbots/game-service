server:
  port: 9006

spring:
  application:
    name: GAME-SERVICE
#  rabbitmq:
#    host: localhost
#    password: guest
#    port: 15672
#    username: guest
#    exchange: user.exchange
#    queue: user.queue
#    routingkey: user.routingkey

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: https://service-registry-cahox.cloud.okteto.net/eureka/
  instance:
    hostname: https://game-service-cahox.cloud.okteto.net
